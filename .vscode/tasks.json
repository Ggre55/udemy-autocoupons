{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "pyupgrade and add-trailing-comma",
      "type": "shell",
      "command": "source ${workspaceFolder}/.venv/bin/activate; pyupgrade ${file} -py311-plus; add-trailing-comma ${file}",
      "problemMatcher": []
    }
  ]
}
